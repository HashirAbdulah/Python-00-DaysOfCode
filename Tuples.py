tup = (1,2,4,6,"English",True,)
print(type(tup))
for i in range(0,len(tup)):
    print(tup[i])
    
    # Tuples are same as Lists(Arrays) but are immutable and can not be changed
    # Tuples can only be changed ,adding,removing only after converting into list and then again for secure lists change into tupel 